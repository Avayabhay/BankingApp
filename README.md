"# BankingApp" 

**Section 2**
	1. Spring initializr to create project
		a. Add spring-web, actuator, h2-databse, devetools, Lombok, validation
	2. Create Accounts microservices
	3. Add codeium - plugin similar to git copilot
	4. Create controller packager and add accountContrller in it with some methods
	5. When we create and run the app, our app starts and all the configurations are automatically handled by springboot - AutoConfiguration feature
Dev-tools reload feature doesn't work - > Need to enable the same in IntelliJ - > Advance Setting -> Allow the automatic reload. Also enable automatic builds in compiler setting
